#ifndef CONFIG_HPP
#define CONFIG_HPP

#include "global.hpp"

// Statically initialized with the config values. Can be changed at runtime,
// but config.cpp must be changed and recompiled for persistent change.
extern Config config;

#endif
